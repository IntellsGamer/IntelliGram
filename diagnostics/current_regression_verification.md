# Current Regression Verification

## 2026-08-25 — Native ordinary send reconciliation

A controlled normal SharedWorker Web K account sent `Web K duplicate-finalization probe 20260825A` to Saved Messages through the visible composer. The browser’s text extraction and a first visual frame appeared to contain the text twice. However, gateway evidence recorded **one** `messages_send_message` request, SQLite contained **one** durable message row (ID `2`, one client random ID), and the Web K SharedWorker message cache contained **one** real message with ID/MID `2` and final flags `{out: true, unread: true}`. The visible second occurrence is therefore not evidence of a server double insert; it requires separate DOM/render interpretation before claiming a duplication defect. The composer’s two overlapping contenteditables were also empty after finalization.

## 2026-08-25 — Native attachment UI attempt

The normal SharedWorker Saved Messages UI exposed its attach control and has one hidden multiple-file input. The browser upload bridge could not locate that hidden input by an interactive index, so the repository-owned `voice.ogg` fixture was **not** injected through the UI in this session. No media message was created by this attempt. The server-side encrypted voice regression independently exercised the same Layer 228 uploaded-document, document retrieval, and persisted-history paths; a separate browser-accessible native upload route is still required for visual voice-bubble confirmation.

## 2026-08-25 — Native broadcast channel setup

Using the normal SharedWorker Web K UI, a controlled account created `Web K Channel Identity Probe` with description `Controlled native channel author verification`. Web K opened the real broadcast channel at `#-2`, showed `1 subscriber`, rendered the native creation event, and exposed the standard Broadcast composer. The Add Subscribers panel remained open on the left after the one-member creation flow; no post has been sent yet in this entry.

## 2026-08-25 — Native broadcast post

A controlled post, `Native channel author probe 20260825B`, was sent through the normal Web K Broadcast composer. The visible channel timeline rendered the post without a personal **You** label, and the sidebar preview attributed it to `Web K Channel Identity Probe`. Gateway evidence recorded one `messages_send_message` request; SQLite has one durable row (`message_id=4`, `peer_id=2`, `sender_user_id=1`, peer kind `channel`). A direct `getMessageByPeer(-2, …)` cache probe resolved the self-peer cache rather than the channel cache, so it is not used as an author-identity assertion. The visible UI, gateway, and durable state support the repair; reload verification remains pending.

## 2026-08-25 — Native Web K voice rendering via client media pipeline

Because the browser upload bridge cannot target Web K’s hidden attachment input, a repository-owned OGG fixture was passed to Web K’s own `appMessagesManager.sendFile` media pipeline with `isVoiceMessage=true`, `duration=2`, and a controlled waveform. This is not a direct MTProto call. The normal SharedWorker UI then rendered the Saved Messages entry as **Voice message** with a visible **0:02** duration and native voice iconography; it did not render as a generic `audio.ogg` file. Gateway, durable metadata, download, and clean-reload checks remain pending in the next observation.

## 2026-08-25 — Native voice clean reload

A cache-busting full document reload into Saved Messages cleared the normal Web K in-memory view and rehydrated history from IntelliGram. The same media entry remained a native voice bubble with its play control and **0:02** duration. This confirms the persisted `documentAttributeAudio` metadata is usable after reload. A failed keyboard reload shortcut inserted a single `R` into the controlled composer; it was removed through the active composer’s ordinary input event, and no message was sent by that cleanup.

## 2026-08-25 — Fresh-load dialog-list anomaly

After the browser session reset to `about:blank`, reopening the normal Web K origin produced a clean shell without a white screen. However, the sidebar listed the persisted broadcast channel while **Saved Messages was absent**, despite the controlled account having durable Saved Messages text and voice rows earlier in the session. This is a reproducible-looking dialog hydration/listing anomaly and is under investigation; it must not be treated as a successful fresh-load result.

### Confirmation

A second cache-busting root load was allowed to settle and still showed only the broadcast channel. A cache-busting direct load to `#1` immediately restored Saved Messages, its text history, and native 0:02 voice bubble. The defect is therefore **root dialog-list hydration** rather than data loss or message-history failure.

### Root dialog-list diagnosis

A live raw `messages.getDialogs` request with Web K’s normal `folder_id=0`, empty offsets, and `limit=20` returned `messages.dialogsSlice` with `count=2`, two dialogs (`peerUser(user_id=1)` and `peerChannel(channel_id=2)`), two top messages, one user, and one chat. The server response is structurally complete. The live client dialog store also reports entries for peer IDs `1` and `-2`, while the root visual list renders only the channel. This narrows the defect to Web K’s folder/index classification or UI projection of the stored self dialog; it is not data loss, getDialogs response truncation, or history hydration failure.

### Client-storage inspection note

The page-to-worker bridge exposes `dialogsStorage.getFolderDialogs(...)` as a `Promise`, even though the imported source returns an array in-worker. Two un-awaited probes therefore failed with `map is not a function`; this is a bridge-shape limitation, not new client evidence. The raw server response and visual root omission remain the relevant facts.

### Root folder projection diagnosis

Awaited client inspection shows `getFolderDialogs(0)` contains only the channel. `getDialogOnly(1)` and `getDialogOnly(-2)` both exist and each reports `folder_id=0`; the self top message is finalized (`{out: true, unread: true}`), so temporary-send state is not the cause. The client’s `canSaveDialog` predicate accepts the self user. The page-to-worker bridge does not expose the internal root index values, so no additional conclusion is drawn from that inspection. The visible root omission is confirmed and requires a compatibility repair or a narrowly documented client-side projection adjustment.

### Final root-list diagnostic

Clearing only the controlled client’s local dialog cache and reloading the normal 20-item dialog page still returned one visible channel despite a server count of two. Calling Web K’s own `processDialogForFilter(selfDialog, filter0)` reported `changed=true` but left `getFolderDialogs(0)` with only the channel. This establishes a root self-dialog index/projection quirk in the current client behavior. A narrow Web K source repair is required to assign a visible root index to the self dialog when normal filtering fails.

### Root-list guard retest

After the client guard was built, a fresh root load created a second sidebar row where Saved Messages was previously absent. However, after settling it remained an unlabeled skeleton rather than a usable Saved Messages item. The guard changes the projection state but does not complete entity/title hydration, so this is **not** a verified fix and further diagnosis continues.

### Root dialog-list repair verified

A fresh cache-busting root load in normal SharedWorker mode now visibly renders **both** durable sidebar dialogs: `Saved Messages` (with the expected `Voice message` preview) and `Web K Channel Identity Probe`. Selecting the actual `#1` Saved Messages sidebar row navigated normally and opened its three-message history, including the persisted `0:02` native voice bubble. The repair is a narrow imported-Web-K storage reconciliation: it obtains the canonical self dialog from Web K's own account/dialog stores and inserts it into the real root folder array with a valid normal dialog index, including when a pre-existing root entry lacks an index. This is not a substitute UI or server-data rewrite.

A second independent cache-busting root load after reopening Saved Messages remained visually correct. Its live SharedWorker root folder contained exactly peer `1` (Saved Messages, top message `5`) and peer `-2` (the broadcast channel, top message `4294967300`), both with `folder_id=0`.

### Native owner-only group creation

The normal SharedWorker Web K `New Group` flow accepted an empty Add Members screen, advanced to Group Name, and created the controlled `Web K Owner-Only Group Probe 20260825` at `#-3`. The resulting real group screen displays **1 member**, a native creation event, the normal group composer, and the sidebar dialog entry. A visible controlled text, `Owner-only group native message probe 20260825A`, appeared as the latest group bubble and sidebar preview. Gateway recorded exactly one `messages_create_chat` and one `messages_send_message`, with no matched protocol failure. Read-only SQLite inspection confirms peer `3` is a `chat`, has exactly one membership (`user_id=1`, `role=owner`), and has exactly one durable row for that message (`id=8`). A cache-busting reload preserved the real sidebar entry, `1 member` count, creation event, and native message. Group Info then opened normally, and its owner Edit view visibly exposes group title, description, private/public type, invite links, reactions, all 13 permissions, administrators, members, history policy, and delete/leave controls. A controlled title change to `Web K Owner-Only Group Updated 20260825` was saved through the native form and immediately updated the active group header, Group Info panel, and sidebar preview. Gateway recorded the normal `messages_edit_chat_title` request; the durable peer title changed accordingly; and a final cache-busting normal SharedWorker reload retained the updated title in both sidebar and group header, alongside the `1 member` count, creation event, and native message. The separate native description field was then set to `Controlled owner-only group description probe 20260825` and saved; Group Info immediately rendered it as the group description. Gateway recorded `messages_edit_chat_about`, the durable peer record retained the description, and after a further cache-busting normal SharedWorker reload the reopened Group Info panel visibly retained the updated title, `1 member` metadata, and description together.

### Native self-profile settings and name edit

The native main-menu Settings route visibly displays the controlled self account’s name, phone number, privacy entry, and one active device. Its real Edit Profile form loads the existing first and last name plus bio and username fields. A controlled first-name change to `WebK QA` was saved through the normal form and immediately updated the Settings header to `WebK QA Regression`. Gateway received `account_update_profile`, and the durable self-user record contains the updated first and existing last name. A fresh cache-busting normal SharedWorker root load then rehydrated `WebK QA Regression` in both the native main menu and Settings screen, with the controlled phone, Privacy and Security entry, and **Devices 1** row intact. The separate optional bio was then set to `Controlled IntelliGram profile bio probe 20260825` through the same native profile form; on save, Settings visibly renders it as a dedicated Bio row. Gateway issued the normal `account_update_profile` request and the durable user record retained the bio. After a further cache-busting normal SharedWorker reload, native Settings visibly retained the updated name, controlled Bio row, phone, privacy entry, and active-device count together.

### Privacy and Security initial visual finding

The native Settings route opens the full Privacy and Security screen. Its static privacy categories render as `Everybody`, but after normal settling the **Blocked Users**, **Connected websites**, **Auto-delete messages**, **Two-Step Verification**, **Passkeys**, and **Who can send me messages?** summaries initially remained visibly `Loading…`. The native `account.getPassword` request was traced to a recovery-context-only SRP lookup that incorrectly rejected an already authenticated MTProto key with `SESSION_PASSWORD_NEEDED`. IntelliGram now securely falls back to that same unrevoked key’s durable account binding. Focused encrypted regressions pass; after gateway restart and a fresh normal SharedWorker Settings → Privacy path, **Two-Step Verification visibly resolves to `On`**. The other listed summaries remain unresolved and are not claimed.

### Native active-device projection

The native Settings → Devices route opens Web K’s Active Sessions page and visibly lists the controlled current session as **This device**, with its IntelliGram identifier, `127.0.0.1 - Self-hosted` location, and `IntelliGram Web K self-hosted` device label. No logout or authorization-reset action has been performed, so session revocation remains unverified.

### Native second-account entry and country picker

Using Web K’s normal **Add Account** menu entry retained the original controlled account and opened the real QR/phone authentication screen under a second account slot. The visible **Log in by phone number** route opened normally; its country picker opened and rendered the expected country list instead of failing or leaving the screen white. The country selection and controlled no-SMS registration path remain in progress.

### Native contact import initial finding

The normal Contacts screen opens empty and exposes the native Add Contact form. A controlled unregistered test number, `+1 515 550 102`, submitted through that form emitted `contacts.importContacts` at the gateway. IntelliGram correctly found no registered target, but Web K left the form open and showed a generic `Unknown error` in its console instead of a clear non-contact result. This unregistered-contact path is **unresolved**; a valid second controlled account is required for the separate contact/private-dialog verification.

### Native channel owner-settings surface

The controlled broadcast channel opens through the normal SharedWorker sidebar with its **1 subscriber** metadata, native creation event, channel-authored post, description, and IntelliGram-hosted private invite link. Its Channel Info and owner Edit views visibly expose channel title/description, private/public type, invite links, reactions, direct messages, discussion linking, recent actions, administrators, subscribers, removed users, auto-translation, message signatures, community linking, and deletion. A controlled title change to `Web K Channel Settings Probe 20260825` was saved through the native form and immediately updated the active header, Channel Info panel, and sidebar entry while retaining the single subscriber and prior channel post. Gateway recorded `channels_edit_title`, and the durable peer title changed accordingly. The separate native description was then set to `Controlled channel settings description probe 20260825` and saved; Channel Info immediately rendered it while retaining the verified title, subscriber count, invite link, and post. Web K issued its supported `messages_edit_chat_about` route for this channel field, and the durable peer record retained the new description. After a cache-busting normal SharedWorker reload, reopening Channel Info visibly retained both the updated title and description, together with the private IntelliGram invite link and channel-authored history. The other exposed channel controls are not yet claimed as verified.

### Fresh-reload group list defect

After a cache-busting normal SharedWorker reload directly into the channel, the channel title correctly rehydrated but the newly durable owner-only group disappeared from the visible root sidebar. Live storage still reports group peer `-3` with `folder_id=0` and top message `8`; however, `getFolderDialogs(0)` contains only Saved Messages (`1`) and the channel (`-2`). This mirrors the earlier self-dialog projection symptom for a group peer.

The imported Web K storage reconciliation was generalized from the self peer to all eligible ordinary folder-0 dialogs, retaining normal filter membership and normal dialog ordering while excluding migrated peers. After a production build and a fresh cache-busting normal SharedWorker root load, the real sidebar visibly displayed the owner-only group, Saved Messages, and channel. Its authoritative root array contained exactly peer `-3` (group), `1` (Saved Messages), and `-2` (channel), all with `folder_id=0`.

### Post-repair ordinary-send visual probe

Through the visible Saved Messages composer, a controlled `SharedWorker post-root repair text probe 20260825C` was sent after the root-list repair. The UI immediately showed one latest message bubble and one sidebar preview, while the composer returned to its empty `Message` state. The gateway recorded one new `messages_send_message` request with no observed protocol failure. Read-only SQLite inspection found exactly one corresponding durable row: `id=6`, `peer_id=1`, `sender_user_id=1`, and one client random ID. A cache-busting normal SharedWorker reload at `#1` retained that text exactly once and preserved the native `0:02` voice bubble. The header initially remained at `3 messages` despite four persisted/rendered message bubbles (anchor, prior text, voice, and the new probe). A direct normal Web K `messages.getHistory` call decoded all four records, so the stale label was traced to the uncounted `messages.messages` response. IntelliGram now returns the standard exact-count `messages.messagesSlice` result. The wire layout was cross-checked against Telegram’s public [`messages.Messages` schema](https://core.telegram.org/type/messages.Messages), which specifies `messages.messagesSlice#5f206716` with flags, count, messages, topics, chats, and users. After gateway restart and a further cache-busting normal SharedWorker reload, the header visibly corrected to **`4 messages`** while retaining all four bubbles and the native voice rendering.

### Native second controlled account, registered contact import, and private dialog

The ordinary Web K **Add Account** flow was completed using controlled local data only. The visible phone-login card’s country picker rendered and, after selecting **USA**, displayed the expected `+1` form state. A new local number proceeded directly to Web K’s native name/password registration view without an external SMS provider. Completing that screen created controlled user `2`, `WebK Contact`, whose own normal SharedWorker root rendered Saved Messages. Read-only storage inspection confirmed the durable non-secret identity fields, and a cache-busting normal SharedWorker reload of `account=2` retained the independent session and root dialog.

The earlier unregistered-number result required no fabricated contact or backend wire change. The Layer 228 `contacts.importedContacts` response already emitted the standard empty imported/users vectors. Imported Web K source converts that empty imported list into its `NO_USER` path; the form’s earlier generic console message remains a client presentation quirk, not evidence that an unregistered record was created. It remains separately noted rather than claimed as a polished no-match experience.

Using the original controlled account’s native Contacts → Add Contact form, the registered `WebK Contact` number was imported successfully. The dialog closed normally, gateway recorded `contacts_import_contacts`, and storage contained the exact controlled mapping from user `1` to user `2`. The Contacts pane nevertheless remained visually empty. Diagnosis showed that IntelliGram’s `contacts.getContacts` handler enumerated only direct-peer memberships and ignored durable `contacts` rows, even though the successful import had persisted. The narrow repair now unions durable imported contacts with the existing direct-dialog contact enumeration, deduplicates results, and derives `mutual` only from a reciprocal import or existing direct-dialog relationship. The encrypted contact-import regression now sends the real `contacts.getContacts` request and asserts that an imported user is returned before a direct dialog exists. The focused regression passed and the complete backend suite passed **62/62**.

After restarting the local gateway, a cache-busting normal SharedWorker root load visibly displayed **WebK Contact** under the Contacts section. The native Contacts screen lists the same contact, and typing `WebK` in its visible search field retained the matching row. Opening the contact through the real sidebar opened a normal private conversation. One unique controlled message, `Registered private contact message probe 20260825`, visibly rendered and promoted the private dialog in the sidebar. Gateway recorded one `messages_send_message`; read-only storage inspection found exactly one matching durable row (`id=10`, direct peer `5`, sender user `1`). A cache-busting normal SharedWorker reload retained the dialog preview and private history for the sender. Switching to user `2` visibly showed the same message as an inbound dialog with an unread badge; opening it cleared the badge and rendered the sender identity. A further cache-busting normal SharedWorker reload of `account=2#1` retained the inbound sidebar preview and history. This verifies the controlled registered-contact import, contact listing/search, one-way private send, recipient delivery, durable storage, and reload behavior; it does not claim all direct-message features such as replies, edits, deletion, forwarding, or media.

## Controlled profile-photo upload, projection, and reload repair — 2026-08-25

A deterministic 320×320 controlled PNG avatar was uploaded through the original account’s native **Settings → Edit Profile** avatar control, Web K’s own cropper, and its visible profile-save action. The local gateway received `upload.saveFilePart` followed by `photos.uploadProfilePhoto`; the upload assembled into one durable `profile_photos` row and set the controlled user’s `profile_photo_id`.

The initial Settings result incorrectly reverted to initials. Investigation showed three concrete compatibility gaps: user entity projections omitted `profile_photo_id`/`userProfilePhoto`; the profile-upload response exposed a zero-dimension photo size; and Web K’s avatar loader used Layer 228 `inputPeerPhotoFileLocation`, which the server did not parse. The narrowly scoped repair projects the stored photo as `userProfilePhoto`, provides a 320×320 profile size in `photos.photo`, decodes and authorizes the peer-photo location, and serves the persisted bytes through `upload.getFile`. A first peer-photo constructor value was corrected against the imported Layer 228 schema after the real loader returned `CONSTRUCTOR_INVALID`.

Encrypted profile-upload/user-photo/file-download regressions passed, followed by the full backend suite (`62 passed, 1 warning`). In normal SharedWorker mode, the second controlled account then loaded the original account’s persisted avatar in the private-dialog sidebar and header after a cache-busting reload; the native **User Info** panel also visibly rendered the same full-size controlled image. This verifies controlled profile-photo upload, durable projection, recipient-side download, and reload rendering. Profile-photo replacement/removal, arbitrary photo dimensions/formats, and photo-message upload remain unverified.

## Controlled channel Sign Messages owner setting — 2026-08-25

The original controlled owner opened the broadcast channel’s native **Channel Info → Edit** surface. Web K’s **Sign Messages** checkbox initially invoked `channels.toggleSignatures` but received `CODE#400 CONSTRUCTOR_INVALID`; the local toggle reverted and gateway tracing confirmed the constructor was not parsed. The imported Layer 228 schema identified `channels.toggleSignatures#418d549c` with flags for `signatures_enabled` and `profiles_enabled` plus `InputChannel`.

The repair adds that parser and owner-authorized handler, a backward-compatible durable `channel_settings.signatures_enabled` column, channel updates and channel-entity flag 11 projection, and a focused encrypted regression covering request acceptance, durable storage, immediate Updates, and a fresh `channels.getFullChannel` re-projection. The focused channel tests passed and the full backend suite passed at `63 passed, 1 warning`.

In normal SharedWorker mode, the repaired native checkbox stayed enabled; the gateway logged `channels_toggle_signatures (0x418d549c)`, and a read-only local database inspection showed `{'peer_id': 2, 'signatures_enabled': 1}`. A cache-busting reload directly into the controlled channel then reopened its native editor with **Sign Messages** enabled and the dependent **Show Authors' Profiles** control visible. This verifies this one owner setting end to end. Reactions, Direct Messages, channel type/username, discussion, actions, and other channel controls remain separately unverified.

## Controlled private photo attachment upload, delivery, and reload — 2026-08-25

In the original controlled account’s normal SharedWorker private dialog with `WebK Contact`, the native **Photo or Video** attachment action was initialized through Web K’s actual composer path, then supplied the deterministic controlled 320×320 PNG. Web K displayed its native **Send Photo** preview; using its visible send control produced one outgoing native image bubble and a `Photo` sidebar preview. Gateway tracing recorded the expected `upload.saveFilePart` followed by `messages.sendMedia` request sequence, with no observed protocol failure.

Read-only durable inspection found exactly one matching outgoing empty-body message (`id=11`, direct `peer_id=5`, `sender_user_id=1`), exactly one related `message_media` row (`kind=photo`, `filename=photo.png`, `mime_type=image/png`, `size=1445`, `file_id=2`), and exactly one persisted file. The stored content length is 1,445 bytes and begins with the PNG signature `89504E470D0A1A0A`; no duplicate outgoing controlled photo row exists.

Switching through Web K’s native account menu to controlled user `2` showed the inbound sidebar preview as `Photo` with an unread badge. Opening the actual private dialog cleared that badge and visibly rendered the full received image bubble. Gateway tracing then recorded `upload.getFile`, confirming recipient-side native image retrieval. Cache-busting normal SharedWorker reloads of both `account=2#1` and the sender’s `account=1#2` each rehydrated the correct photo sidebar preview and image bubble without duplication. This verifies this controlled PNG private-photo route from native UI selection through upload, durable media/file state, recipient retrieval/rendering, and both-side reload. It does not claim arbitrary photo formats/dimensions, document attachments, media editing, or a separate explicit photo open/save/download interaction.

## Controlled Web K message acknowledgement, deduplication, and native edit recovery — 2026-08-26

The local runtime for this verification deliberately remained **HTTP**, as required. In that mode IntelliGram’s pushed compatibility bootstrap bypasses Service Worker registration and uses the in-process MTProto/crypto worker fallback; this is therefore not presented as a normal HTTPS Service Worker or SharedWorker assertion. A complete backend regression run after the final message-state changes passed **64 tests** with one existing FastAPI/Starlette deprecation warning. The associated Web K client typecheck and production build had also passed after the reconciliation changes.

The repair persists a sender-only client random ID in the durable sender update envelope and includes `updateMessageID(message_id, random_id)` before that sender message in `updates.getDifference` replay. The imported Web K update manager now permits that random-ID mapping and its zero-PTS final `updateNewMessage` acknowledgement while initial synchronization is active, while retaining normal PTS-bearing update holdback. The bubble renderer also retains an already-mounted authoritative final bubble instead of leaving an optimistic temporary bubble alongside it; this addresses the demonstrated `history_update` / `message_sent` DOM race without creating duplicate SQLite messages.

A controlled post-reload ordinary send, `Post-reload native reconciliation probe 20260826E`, was submitted through the visible Saved Messages composer using its normal Enter path. The live timeline immediately showed exactly one outgoing final bubble with `data-mid="17"`, the sidebar showed exactly one matching preview, the real composer was empty, and the chat header showed **10 messages**. Read-only SQLite inspection found exactly one matching durable sender row: ID `17`, peer `1`, sender `1`, with client random ID `153766181914739344`. A second cache-busting reload into `#1` rehydrated exactly one bubble with the same final MID, one sidebar preview, and an empty composer. This verifies the controlled HTTP-fallback first-send/reload surface; it does not claim general multi-device synchronization or every retry edge case.

The edit protocol parser was corrected to the Layer-228 `messages.editMessage#b106e66c` request constructor. From the normal visible Web K UI, ArrowUp opened edit mode on finalized message ID `16`; the replacement `Native edit submit verification 20260826D` was entered into the actual contenteditable and submitted through the visible native edit/send button. The original bubble changed in place, the sidebar preview updated, and edit mode closed. Live DOM inspection found exactly one matching timeline bubble with Web K’s `.time-edited` marker and one sidebar preview. SQLite confirmed the same message ID `16` retained its original client random ID, had the replacement body, a non-null `edited_at`, and had no duplicate matching body row. A final cache-busting reload again rendered exactly one bubble with the edited text and visible `edited` marker. This verifies controlled text edit submission, durable projection, and reload presentation; deletion, forwarding, replies, context-menu variants, and other message-management operations remain unverified.
